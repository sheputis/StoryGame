install node_modules by running "npm install"?

to start the server, type : node index.js (the game will be accessed on the browser "http://localhost:8080/")

To play on the internet, it is easy to forward the port to an external
server by using, for example, ngrok.
